{
  "name": "zikr",
  "version": "1.0.0",
  "private": true,
  "description": "Quran daily tracker and reader - one Juz a day on a 30-day cycle anchored to Rabi' II 1.",
  "scripts": {
    "build": "node build.js",
    "test": "node build.js --check && node --test test/core.test.js",
    "test:e2e": "node test/browser.test.js",
    "serve": "python3 -m http.server 8000",
    "serve:dist": "python3 -m http.server 8000 --directory dist"
  },
  "devDependencies": {
    "playwright-core": "^1.63.0"
  }
}
